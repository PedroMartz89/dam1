function potencia(base, exponente){

    let resultado = 1;

    for (let i = i; i <= exponente; i++) {

        resultado = resultado * base
    }

    return resultado
}

let calculo = potencia(3,1);
console.log(calculo);