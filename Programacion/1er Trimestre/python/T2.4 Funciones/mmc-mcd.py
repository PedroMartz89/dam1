def mcm(n,m):

    temp = n * m

    return (temp / mcd2(n,m))  

def mcd2(n,m):

    listan= []
    listam= []

    for i in range(1, n+1):
        resti

    if (len(listam) >= len(lsitan)):

        for elem in listam:
            if elem in listan:
                mayor=elem
        
        else:
            for i in listam:
                
                if elem2 in listam:
                    mayor=elem2

    return(mayor)